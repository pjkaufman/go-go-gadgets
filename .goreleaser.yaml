# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: cat-ascii
    ldflags: -s -w
    main: ./cat-ascii/main.go
    env:
      - CGO_ENABLED=0
    binary: cat-ascii
    goos:
      - linux
      - darwin
      - windows

  - id: epub-lint
    ldflags: -s -w
    main: ./epub-lint/main.go
    env:
      - CGO_ENABLED=0
    binary: epub-lint
    goos:
      - linux
      - darwin
      - windows

  - id: jp-proc
    ldflags: -s -w
    main: ./jp-proc/main.go
    binary: jp-proc
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows

  - id: magnum
    ldflags: -s -w
    main: ./magnum/main.go
    binary: magnum
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows

  - id: song-converter
    ldflags: -s -w
    main: ./song-converter/main.go
    binary: song-converter
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows

  - id: versy
    ldflags: -s -w
    main: ./versy/main.go
    binary: versy
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows

archives:
  # cat-ascii binary only
  - id: cat-ascii-archive
    ids:
      - cat-ascii
    format: binary
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
      {{- if eq .Os "windows" }}.exe{{ end }}

  # epub-lint binary only
  - id: epub-lint-archive
    ids:
      - epub-lint
    format: binary
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
      {{- if eq .Os "windows" }}.exe{{ end }}

  # jp-proc binary only
  - id: jp-proc-archive
    ids:
      - jp-proc
    format: binary
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
      {{- if eq .Os "windows" }}.exe{{ end }}

  # magnum binary only
  - id: magnum-archive
    ids:
      - magnum
    format: binary
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
      {{- if eq .Os "windows" }}.exe{{ end }}

  # song-converter binary only
  - id: song-converter-archive
    ids:
      - song-converter
    format: binary
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
      {{- if eq .Os "windows" }}.exe{{ end }}

  # versy binary only
  - id: versy-archive
    ids:
      - versy
    format: binary
    name_template: >-
      {{ .Binary }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
      {{- if eq .Os "windows" }}.exe{{ end }}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).